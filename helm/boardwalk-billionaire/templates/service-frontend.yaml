apiVersion: v1
kind: Service
metadata:
  name: {{ include "boardwalk.fullname" . }}-frontend
  labels:
    {{- include "boardwalk.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "boardwalk.frontendSelectorLabels" . | nindent 4 }}
